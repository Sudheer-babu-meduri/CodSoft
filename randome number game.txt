package com.loops;
 import java.util.Random;

import java.util.Scanner;

  public class GuessRandomNumber {

	public static void main(String[] args) {
		 Scanner s=new Scanner(System.in);
		 
		    boolean playAgain = true;

	        while (playAgain) {
	            Random random = new Random();
	            int number = random.nextInt(10) + 1; // random number between 1 and 5
	            int attempts = 1;
	             
            while (attempts <=3 ) {
                System.out.println("Enter your Guess (1 to 10):");
                int n = s.nextInt();
                
                 
                if (number == n) {
                    System.out.println(" Correct! You guessed in " + attempts + " attempts.");
                     
                    break; // stop guessing if correct
                } else if (number > n) {
                    System.out.println(" Your Guess is too Low");
                } else {
                    System.out.println(" Your Guess is too High");
                }
                attempts++;  
                if (attempts>= 4) {
                    System. err.println("You Failed,have used all 3 chances! The number was: " + number);
                }  
            }

            System.out.println("Do you want to play again? Say 'Yes' or 'No'");
            String status = s.next();
            if (status.equalsIgnoreCase("no")) {
                playAgain = false;
                System.out.println("Thank you for playing!");
            }
        }

        s.close();
	}
  }         
       